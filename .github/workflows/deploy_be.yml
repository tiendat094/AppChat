name: Deploy App_Chat Be
on:
  pull_request:
     branches:
      - main
  push:
    branches:
      - main
    
jobs:
  build:
    runs-on: self-hosted
    name: Build Java
    environment: main
    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Build Java
      run: |
       cd /actions-runner/_work/AppChat/AppChat/BE
       

 
        
    
    
    
